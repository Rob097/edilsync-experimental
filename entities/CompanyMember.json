{
  "name": "CompanyMember",
  "type": "object",
  "properties": {
    "company_id": {
      "type": "string",
      "description": "Reference to Company"
    },
    "user_id": {
      "type": "string",
      "description": "Reference to User"
    },
    "user_email": {
      "type": "string",
      "description": "User email for lookup"
    },
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "member"
      ],
      "description": "Role within the company"
    },
    "profession": {
      "type": "string",
      "enum": [
        "general",
        "architect",
        "engineer",
        "surveyor",
        "designer",
        "accountant",
        "other",
        "owner_admin",
        "project_manager",
        "site_manager",
        "crew_leader",
        "technical_office",
        "safety_manager",
        "worker",
        "backoffice",
        "external_consultant"
      ],
      "description": "Professional role"
    },
    "company_member_role": {
      "type": "string",
      "enum": [
        "owner_admin",
        "project_manager",
        "site_manager",
        "crew_leader",
        "technical_office",
        "safety_manager",
        "worker",
        "backoffice",
        "external_consultant"
      ],
      "description": "Operational role of the member inside the company"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "invited",
        "inactive"
      ],
      "description": "Membership status"
    }
  },
  "required": [
    "company_id",
    "user_email",
    "role"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "company_id": {
            "$in": "{{user.data.admin_company_ids}}"
          }
        },
        {
          "created_by": "{{user.email}}"
        }
      ]
    },
    "read": {
      "$or": [
        {
          "company_id": {
            "$in": "{{user.data.company_ids}}"
          }
        },
        {
          "user_email": "{{user.email}}"
        },
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "company_id": {
            "$in": "{{user.data.admin_company_ids}}"
          }
        },
        {
          "created_by": "{{user.email}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "company_id": {
            "$in": "{{user.data.admin_company_ids}}"
          }
        },
        {
          "created_by": "{{user.email}}"
        }
      ]
    }
  }
}